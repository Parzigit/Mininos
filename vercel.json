{
  "framework": "vite",
  "buildCommand": "cd dlbmt/frontend && npm install && npm run build",
  "outputDirectory": "dlbmt/frontend/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
